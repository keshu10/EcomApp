import { ResponsiveTableDirective } from './responsive-table.directive';

describe('ResponsiveTableDirective', () => {
  it('should create an instance', () => {
    const directive = new ResponsiveTableDirective();
    expect(directive).toBeTruthy();
  });
});
